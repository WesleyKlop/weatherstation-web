import { createStore } from 'vuex'

/** @type {StoreState} */
const initialState = {
    stats: null,
}

export default createStore({
    state: initialState,
    mutations: {},
    actions: {},
    modules: {},
})
